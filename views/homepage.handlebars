{{! TODO - write the template to show all posts on the / route }}
{{! done  }}
<header>
  <h1
    class="mirror"
    data="Homepage"
    style="position: absolute; top: 55; left: 0; font-size: 40px"
  >Homepage</h1>
</header>
<h1>All Posts</h1>
{{#each posts}}
  <div class="post">
    <h3>{{title}}</h3>
    <h3>{{body}}</h3></div>
{{/each}}